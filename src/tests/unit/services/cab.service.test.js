import { describe, it } from 'vitest';

/**
 * Placeholder for cab.service unit tests.
 * Add mocks for prisma + capacity checks when expanding coverage.
 */
describe.skip('cab.service', () => {
  it.todo('rejects assignment when cab is at capacity');
  it.todo('rejects assignment for non-CONFIRMED guest');
});

import { describe, it } from 'vitest';

describe.skip('qr.service', () => {
  it.todo('returns alreadyCheckedIn without duplicate audit on second scan');
  it.todo('rejects scan when qrEnabled is false');
});
